# Render Deployment Configuration

services:
  - type: web
    name: water-forecasting-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api_server.py"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
